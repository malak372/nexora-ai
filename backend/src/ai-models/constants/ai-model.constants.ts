/**
 * Default fallback priority assigned to a new AI model.
 *
 * @author Malak
 */
export const DEFAULT_AI_MODEL_PRIORITY = 0;

/**
 * Default weighted-routing value assigned to a new AI model.
 */
export const DEFAULT_AI_MODEL_WEIGHT = 1;

/**
 * Default maximum output-token limit.
 */
export const DEFAULT_AI_MODEL_MAX_OUTPUT_TOKENS = 2_048;
