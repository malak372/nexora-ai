import { getApiErrorMessage, normalUserApi } from '../../shared/api/normalUserApi';
const unwrap=(r)=>r?.data?.data??r?.data;
export async function getBusinessModelTemplates(){try{const r=await normalUserApi.get('/business-model-templates');const p=unwrap(r);return Array.isArray(p)?p:p?.data??[];}catch(e){throw new Error(getApiErrorMessage(e,'Business-model templates could not be loaded.'));}}
export async function getCurrentBusinessModel(ideaId){try{return unwrap(await normalUserApi.get(`/users/ideas/${ideaId}/business-models/current`));}catch(e){if(e?.response?.status===404)return null;throw new Error(getApiErrorMessage(e,'The business model could not be loaded.'));}}
export async function generateBusinessModel(ideaId,businessModelTemplateId){try{return unwrap(await normalUserApi.post(`/users/ideas/${ideaId}/business-models`,{businessModelTemplateId}));}catch(e){throw new Error(getApiErrorMessage(e,'The business model could not be generated.'));}}
export async function openBusinessModelPreview(ideaId){try{const r=await normalUserApi.get(`/users/ideas/${ideaId}/business-models/current/preview`,{responseType:'blob'});const url=URL.createObjectURL(new Blob([r.data],{type:'text/html'}));window.open(url,'_blank','noopener,noreferrer');setTimeout(()=>URL.revokeObjectURL(url),60000);}catch(e){throw new Error(getApiErrorMessage(e,'The preview could not be opened.'));}}
export async function downloadBusinessModel(ideaId){try{const r=await normalUserApi.get(`/users/ideas/${ideaId}/business-models/current/download`,{responseType:'blob'});const url=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=url;a.download='nexora-business-model.html';document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);}catch(e){throw new Error(getApiErrorMessage(e,'The business model could not be downloaded.'));}}
